# CleanClaudeConfig

Simple utility to clean up your ClaudeCode config.

- safely remove projects from ./claude/projects and claude.json iwhen their directories are no longer existant
- removing local configuration that is already in global / user-scoped config
